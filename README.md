# toc_project
Django website for regular expression and context free grammar.

install the python packages 
1. Django 
2. pyformlang

use the command python manage.py runserver to switch on the django server

In regular expression 
input should be given the form of concatenation enclosed in simple brackets 
union enclosed in square brackets.

In grammar - In place of epsilon , the given space should be left blank.
